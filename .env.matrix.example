# Matrix ISMS Configuration
# Copy to .env and customize for your deployment

# Matrix Synapse Homeserver
MATRIX_SERVER_NAME=matrix.openami.local
MATRIX_HTTP_PORT=8008
MATRIX_FEDERATION_PORT=8448
MATRIX_NO_TLS=true
MATRIX_ENABLE_REGISTRATION=false
MATRIX_REPORT_STATS=no
MATRIX_UID=991
MATRIX_GID=991

# Matrix PostgreSQL Database
MATRIX_POSTGRES_DB=synapse
MATRIX_POSTGRES_USER=synapse
MATRIX_POSTGRES_PASSWORD=synapse_secure_password_CHANGE_ME_IN_PRODUCTION
MATRIX_POSTGRES_PORT=5433

# Element Web Client
MATRIX_ELEMENT_PORT=8888

# ISMS Configuration
# These settings enforce compliance requirements:
# - End-to-end encryption by default
# - 10-year message retention (EU AI Act Article 12)
# - Registration disabled (admin provisioning only)
# - Federation disabled (security isolation)

# Quick Start:
# 1. Copy this file to .env
# 2. Change MATRIX_POSTGRES_PASSWORD
# 3. Run: docker-compose -f docker-compose.services.yml --profile isms up -d
# 4. Access Element at http://localhost:8888
# 5. Create admin user: docker exec -it ami-matrix-synapse register_new_matrix_user -u admin -p password -a http://localhost:8008

# See compliance/config/matrix/README.md for detailed setup instructions
